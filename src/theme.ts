import { BRAND_LIGHT } from "./brand.js";

// The toolbar badge's resting colour (a badge has one colour for both schemes).
export const VIBEWAITING_NEUTRAL = BRAND_LIGHT["surface.inverse"];
export const VIBEWAITING_RADIUS = "12px";
